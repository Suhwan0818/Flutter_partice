import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
      title: 'Flutter demo',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Sean Flutter Test  '),
        ),
        body: Center(
          child: Text('Hello World'),
        ),
      )));
}
